[pytest]
markers =
    smoke: marks tests as smoke tests (fast end-to-end flow tests for local dev)
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
