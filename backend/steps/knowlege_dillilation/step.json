{
  "step_type_key": "knowlege_dillilation",
  "display_name": "Model dililation",
  "description": "Runs a trained model on new data to generate predictions.",
  "inputs": [
    {
      "name": "model",
      "type": "pytorch_model"
    },
    {
      "name": "images",
      "type": "image_dir"
    }
  ],
  "outputs": [
    {
      "name": "predictions",
      "type": "inference_results"
    }
  ],
  "config_schema": {
    "confidence_threshold": {
      "type": "float",
      "description": "Minimum confidence score to retain a prediction.",
      "default": 0.5
    },
    "device": {
      "type": "string",
      "description": "Compute device to use (cpu or cuda).",
      "default": "cuda"
    },
    "color": {
      "type": "string",
      "description": "Compute device to use (cpu or cuda).",
      "default": "cuda"
    }
  }
}