# Secrets — never commit real credentials
.env
.env.local

# Python / uv
.venv/
__pycache__/
*.pyc
.python-version

# Mock Tapis local job state
tapis_jobs.json

# Tapis Expanse SSH credential (dedicated key — never commit)
.tapis_expanse_key
.tapis_expanse_key.pub
